import { addClass, removeClass } from "../util/domUtils.js";

export default class HeaderAligner {
  constructor(templateID) {
    this.templateID = templateID;
  }
/*   alignHeader(header) {
    if ()
  } */
}